const checkbox = document.querySelector('.checkbox');

    checkbox.addEventListener('clic',  function()) {
  element.classList.remove('btn-disabled');
  element.classList.add('btn-disabled');
}
    
